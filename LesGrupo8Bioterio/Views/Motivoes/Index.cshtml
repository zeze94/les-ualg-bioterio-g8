@model IEnumerable<LesGrupo8Bioterio.Motivo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TipoMotivo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.NomeMotivo)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoMotivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeMotivo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMotivo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMotivo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMotivo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
